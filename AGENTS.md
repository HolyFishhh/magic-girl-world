# 项目开发约定

## Git 保存与版本支持（2026-09-18 用户长期要求）

- 后续更新默认使用 Git 保存。每个完成并验证的任务或独立阶段及时提交本任务的源码、配置、测试和文档，不积压大量只存在于本地工作区的改动；本地提交已获长期授权，无需逐次确认。未完成的阶段如需保存，提交说明必须标明未完成项和验证状态。
- 提交前检查差异，只纳入本任务改动及必要依赖；保留其他工作树内容，不提交密钥、玩家存档或本机临时证据。收尾报告提交号与验证结果。推送、安装和正式发布按对应任务已有授权执行，保存 Git 提交本身不触发发布。
- 后续每次完成并验证更新后，默认同步更新本地测试酒馆中当前最新版本的角色卡；按隔离构建、备份与哈希核验流程执行，不覆盖旧版本角色卡，不自动刷新页面、生成内容、推进聊天或改写玩家存档。收尾必须明确告知用户应测试的角色卡显示名及精确头像文件名。若本地酒馆不可用或安装验证失败，必须如实说明，不能把源码完成当作本地更新完成。
- 项目以最新版本的内容和协议为维护目标，不默认承诺旧角色卡、旧扩展、旧存档或旧 DSL 的向后兼容。1.0.2 之后的下一版本必须清理不再需要的旧版兼容分支、迁移、别名和兜底入口，不为历史写法继续扩大实现。
- 清理前确认最新正式入口的实际依赖；保留最新内容的生成、校验、有限修复、执行、交互、保存恢复与说明链，以及当前支持的浏览器/模型接口适配。仅为旧版本存在的测试随实现清理，不能删掉最新行为的回归来获得通过。
- 停止支持的旧数据明确提示不支持或需要新开局，不猜测转换，不删除或改写玩家原存档。具体清理范围和升级影响写入下一版本更新记录。

## 新机制必须同步完成配套

- 新增或修改卡牌、状态、资源、召唤物、选择器、永久成长、奖励等机制时，必须在同一任务内检查：公开 Prompt/世界书 → JSON Schema → 解析/编译 → 校验与有限修复 → 执行与交互 → 存档结算/恢复 → 中文说明与 UI 渲染。不能只完成运行时代码就宣布功能完成。
- 显示必须来自可执行结构；中文名称通过当前作用域内的卡牌、状态、资源、召唤物和姿态定义解析。新增枚举、字段、公式和筛选器要同步两条说明链（`contentDescription.ts` 和 `effectDisplay.ts`），不得漏条件、目标、范围、次数、期限、未来副本或实例身份。
- 检查受影响的奖励/开局/事件/商店/营火预览、牌组与牌库、战斗手牌与详情、状态/召唤物面板。规则使用共享胶囊组件；独立特性单独显示，关联条件、效果与期限保持一组。长内容换行，完整预览不得截断；引用说明与选择器必须可操作。
- 资源/能量使用“获得”，状态和能力使用“赋予”；资源图标从对应所属方的资源定义读取，不能一律使用能量图标。公式化简仅用于显示，保留必要下限且不得回写执行树或存档。战斗状态与能力共用一栏并用外框区分，层数放在状态图标右下角；验证窄屏换行和完整说明。
- 卡牌标题采用统一布局：“唯一”等独立词条紧邻卡牌类型，类型/词条行高度固定。词条数量不能改变卡名或规则区的位置；多词条保持横排且可访问，不能因附加词条换行。
- 新机制字段要同步共享字段表、投影、修复白名单及修复 Schema，防止出现正式协议支持但修复器拒绝或悄悄丢字段的情况。
- 对机制变更添加有意义的端到端契约回归：至少证明合法输入能编译和执行、保存后语义不变、显示内容准确，覆盖选择/取消等涉及的交互。低影响纯样式改动用现有检查即可，不写只复述实现的测试。
- 收尾说明验证了哪些链路；无法进行浏览器视觉或真实模型验证时明确记录限制，不把离线检查说成实玩验收。配套未完成时继续实现或如实列为未完成，不能默认留待下次。

详细机制规范和边界见 [机制契约与长期回归](docs/mechanism-contract-regression.md)、[后端边界](docs/backend-boundaries.md)；当前安装与接续状态见 [项目交接](docs/PROJECT_HANDOFF.md)。

## 既有产品边界

- 卡组数量、构筑、输出/生存能力、创意与数值质量只通过 Prompt 引导；程序不擅自改写合法卡牌或添加质量硬门槛。仅实际无法执行的结构错误允许硬拦截，修复限定在已证明的错误槽位。
- “开始爬塔”直接启动；“保存当前预设”位于开始按钮上方，点击保存，不弹出二次确认。
- 保留已有工作树和玩家存档。构建与本地安装按隔离构建、备份、哈希核验流程执行；不自动重生成、领取奖励、推进当前存档或刷新用户页面。Git 保存和发布授权按上方长期要求执行。
