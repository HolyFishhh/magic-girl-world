# 剧情模式长期目标验收：0.5.112

## 验收目标

在 SillyTavern + Tavern Helper + MVU 的真实剧情聊天中连续完成至少两场战斗，验证首轮初始化、自然战斗触发、出牌与结算、成长、奖励、持久化和最近楼层渲染。爬塔模式不在本批次范围内。

## 实机环境

- SillyTavern：1.18.0
- Tavern Helper：本地已安装版本
- MVU：角色卡内嵌版本
- 模型：deepseek-v4-flash
- 基线角色卡：魔法少女世界 0.5.111
- 聊天：story-loop-0-5-111

## 已通过证据

| 项目 | 结果 |
| --- | --- |
| 首轮完整初始化 | 12 张牌、1 遗物、1 道具、1 个显式自定义状态 |
| 第一场战斗 | 胜利；生命 100→86；经验 0→25 |
| 第一场奖励 | 3 选 1 卡牌、1 选 1 道具；领取后候选与限制清空 |
| 第二场触发 | 由后续剧情自然注册不同敌人并进入战斗页 |
| 第二场战斗 | 胜利；生命 86→64；经验 25→50 |
| 第二场奖励 | 领取后永久内容为 14 张牌、1 遗物、3 道具 |
| 多轮清理 | 两场敌人、临时牌区和奖励候选均未残留或重复结算 |
| 刷新与重开 | 最近聊天重开后生命、欲望、经验、牌组、遗物和道具一致 |
| 楼层渲染 | 只保留最近窗口内需要显示的状态栏；没有重复战斗页 |

## 本批次修复

`0.5.111` 实战暴露状态触发器的省略目标反转。`0.5.112` 将状态 `triggers` 内省略的 `to` 定义为状态持有者，并保留显式 `to:"opponent"`。状态名称、ID 和机制仍全部由 AI 显式注册，运行时没有任何内置状态。

## 发布门禁

完整 `npm run release:tavern` 已通过，用时 99.1 秒。角色卡以独立名称 `魔法少女世界 0.5.112.png` 导入本地酒馆，确认嵌入世界书 `魔法少女世界0.5.112` 与角色运行时生效。

真实状态夹具 `battle-repair-status-2026-08-25T14-52-32Z` 使用同一份无 `to` 的 `apply/tick` 定义完成双向验证：

- 玩家持有时生命 `40 -> 38`，敌人保持 `36`。
- 敌人持有时生命 `36 -> 34`，玩家保持 `38`。
- 回合末双方各再损失 3；敌方行动另使玩家损失 6，最终玩家 `29/80`、敌人 `31/36`。
- 整页刷新并从最近聊天重开后仍为回合 2、双方各 1 层状态、玩家 `29/80`、敌人 `31/36`，牌区和能量一致。

结论：本长期目标通过。剧情模式的双战成长闭环与状态归属修复均已在真实 SillyTavern 中验收。
