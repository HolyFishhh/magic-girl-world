# 2026-09-19 剧情状态恢复与爬塔战后剧情

## 修复
- 对照既有剧情变量模板与旧面板：统一状态栏隐藏了旧面板，却没有接入剧情字段。现在在统一状态栏读取并显示衣物、背包、永久/临时剧情状态、势力关系、NPC 好感与人物记录；爬塔模式不显示这些字段。
- 显式新建角色时按模式投影 MVU 模板：剧情保留模拟字段，爬塔移除剧情专属字段。已有锁定存档不迁移、不改写；直接爬塔开局事务也只在排除既有开局后移除模板 NPC/势力字段。
- 爬塔战后剧情默认开启，可在设置关闭。结算原子保存日志快照，扩展用当前剧情预设独立生成正文，在路线图上方展示；换节点后进入历史。
- 独立请求队列不占用节点生成队列；后续敌人/事件生成仅参考最近完成的剧情，不等待剧情、不要求强行关联。
- 程序持有 tower_battle_stories（最多24条），同步初始化模板、MVU 字段 Schema、变量结构说明。生成失败不撤销战斗、不重复奖励、不写模型变量、不推进路线。

## 验证
- 应用与扩展 TypeScript 检查。
- 模式变量/只读显示/保存恢复、战后生成默认及关闭/失败/恢复/切聊天/去重、独立队列、后续上下文。
- 战斗结算适配器、爬塔战斗奖励结算、酒馆续写、MVU 契约矩阵、世界书契约、生成宿主回归。
- 隔离 Edge 当前源码测试：390/1000px 的剧情字段与模式隔离、无横向溢出、保存恢复、战后等待提示、完整正文、路线图上方位置与历史。检查窄屏截图并修正模板空白引起的过大间距。
- 旧 test-story-layout-v428.mjs 的旧导入 mock 与现有统一状态栏不匹配，不能作为此次布局验收；由新当前源码浏览器回归覆盖相关行为。

## 边界
- 未进行真实模型或现有玩家存档实玩；模拟生成验证不代表真实模型文风/时延验收。
- 未安装、发布、刷新用户页面，也未替玩家生成内容或推进存档。
- 修复显示现有存档字段，不凭空重建已经缺失的玩家数据。
- 既有工作树改动保留，不纳入本任务提交。隔离构建证据保留在 tmp，不提交构建产物或本机证据。

## 隔离构建结果与已知检查限制
- 首轮扩展/runtime Webpack 生产构建、runtime/interface 导出、扩展打包检查通过。
- 角色 runtime 回归的设置默认值断言已补上新增开关；继续运行后停在启动壳长度检查：start 壳为6638字符，超过既有6000字符门槛。此壳由本任务未修改的 viewBootstrap.ts 生成（工作树原有挂载保护改动仍保留），未放宽断言，也不宣称完整构建验收全绿。
- 最后补上独立战后队列的终态缓存清理，专项回归证明已释放；扩展最终隔离重建与打包检查通过。runtime 源码与首次成功构建时一致。
- SHA256 检查确认1028个 dist/现有安装文件未变化。详情见本机 tmp/story-aftermath-protection-check.json；未安装候选产物。
