# 剧情模式原布局恢复

用户反馈爬塔模式更新后，剧情模式原有内容全部不可见，后续恢复又把酒馆已经显示的剧情正文重复包进了状态界面。调查确认世界书、剧情开场、NPC、势力、衣物与背包数据并未删除；问题来自公共页面被新的爬塔统一状态栏接管，以及剧情模式误用了爬塔的正文面板。

## 本轮修复

- common + story 只恢复原状态栏、人物关系、势力关系、卡牌资源和自由行动布局；剧情正文继续由酒馆消息直接显示，不创建 `#mwg-story-panel`，战斗时只切换到战斗页面。
- 修复通用界面正则曾把整条助手消息替换成 iframe 的根因：剧情模式现在只替换消息末尾的 `UpdateVariable`、内容交接和状态占位符，正文仍由酒馆原生 Markdown 渲染，状态栏接在正文后方。
- 爬塔控制器改用内部 `<TOWER_STATUS/>` 标记，通用正则仅对该标记执行整条消息接管，因此爬塔继续使用自己的正文、节点与历史面板，不会因为剧情模式恢复原生正文而出现双份内容。
- 只有 tower 才创建正文面板和统一状态栏，并搬移地图、节点和奖励。剧情模式不会读取 `run_node`、`completed_expedition`、`mwg_tower_initial_commit` 或战后剧情缓存，也不消费路线事务或唤醒待生成的幕间开场。
- 原卡牌资源面板继续使用当前结构化卡牌与规则说明，并补充能力、当前战斗状态及层数、资源、我方欲望效果、当前姿态和召唤成长。
- `battle.player_lust_effect` 继续接受有效的结构化效果，但空名称、空描述和 `$meta` 组成的 MVU 可选模板视为未提供，不再强制生成欲望效果，也不会触发修复。
- 当前模型输出中出现的 `eff果s:null` 仅在同张卡已经存在合法 `effects` 时作为无语义污染移除；非空 `eff果s` 仍保留给严格校验报错，未知字段规则没有整体放宽。
- 现有剧情字段和存档只读保留，不重建、不迁移、不推进玩家存档。设计辅助器的剧情 MVU 上下文、有限修复和持久写回路由经检查无需修改。

## 验证

- TypeScript：app 与 SillyTavern 扩展类型检查通过。
- 契约：剧情模式恢复、公共界面、剧情长流程、爬塔战后、开始页、消息历史保护和 Tavern 续写测试通过。
- 浏览器：隔离 Edge 在 390px 与 1000px 验证剧情模式从原状态栏直接开始、不存在重复正文面板，详情展开、长文换行、模式隔离、保存恢复只读，以及爬塔战后正文与历史不回归。
- 实际消息替换：只读获取当前聊天的 6,227 字符助手消息并应用待发布正则，确认开场及正文原样保留、变量块和状态标记不显示、正文后只生成一个状态栏 iframe；没有写回聊天。
- 实际存档：只读获取 `魔法少女世界 1.0.3 - 2026-09-19@19h40m56s409ms`，确认前三张卡均为 `eff果s:null` 与合法 `effects` 并存，欲望效果为空模板；当前归一化后未知字段和空模板均不存在，严格战斗预检 0 项错误。未调用保存接口。
- 构建：从源码提交 `70e41c0` 使用独立 `tmp/story-mode-runtime-70e41c0` 完成生产构建、Tavern runtime/interface 导出和角色运行时产物检查；仅有既有 bundle 体积警告。

## 本地安装

- 已将隔离构建中的运行时和四个界面壳安装到本地最新角色卡 `魔法少女世界 1.0.3`（头像文件 `魔法少女世界 1.0.3.png`），没有改版本、世界书、MVU 脚本、聊天或全局正则。
- 安装后 API 回读运行时 `magic-girl-world-runtime-1-0-3`，SHA-256 为 `5ccc0e1d3e01201749c291fad4e0fa78644b8c76a231553581bf2d5460450ae4`，与 3,951,342 字节隔离产物一致；开始、变量更新展示、通用和战斗四个界面壳均存在。
- 安装器备份位于 `tmp/tavern-runtime-backups/ee6e29fe-4e65-4b73-b862-09d7e0ee46ef`。安装前后的当前聊天均为 `魔法少女世界 1.0.3 - 2026-09-19@19h40m56s409ms`，6 条记录未变；酒馆当前选择为 `魔法少女世界 1.0.3.png`，没有自动刷新页面。

未调用真实模型，也未推进或改写任何玩家存档；离线、实际存档只读预检与隔离浏览器验证不等同于真实模型实玩验收。
