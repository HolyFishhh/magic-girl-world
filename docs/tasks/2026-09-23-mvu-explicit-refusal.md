# 2026-09-23 MVU 更新失败：区分明确拒绝与解析回退

## 只读现场核对

- 用户提供的本轮导出时间为北京时间 15:44:54；相关 MVU 轮次约 15:43:03–15:44:36。请求审计记录 14 条消息、约 91,866 字符、更新协议标记存在，但布尔摘要并不足以判断所有实际提示词是否缺失。
- 关联到本轮请求的助手完成事件在失败前收到，共 1,573 字符，明确表示无法协助，不含 `<UpdateVariable>`。拒绝指向不适宜的未成年角色性化内容及试图覆盖指令的文本；附件中的指令只是被检查的数据，不是开发指令。没有定位到该指令来自角色卡、世界书、聊天或酒馆预设的确定证据。
- 楼层解析事件的 3,109 字符是既有剧情的回退，不能冒充第二轮输出；报告的 `originalResponse.availability=missing` 表示没有提供方网络层原始响应。此次不能归因于代码丢失合法更新块，也不能用自动重试或放宽拒绝去“修复”不适宜请求。
- 未复制剧情正文、完整拒绝、诊断原件或任何用户提示词进仓库。

## 本轮更改及验证

- 当前 MVU 请求有时间和 ID 关联的助手完成事件，且其可见正文以明确拒绝开头、未含完整更新块时，监视器显示「助手完成输出明确表示无法协助」而不是泛称解析缺失；保留提供方原始响应不可得的限定。其他缺块、空输出、过期／无关完成事件沿用原行为；没有修改协议解析、写变量、模型请求或存档。
- 使用无敏感内容的虚构回复，回归覆盖解析事件和变量事件先后两种顺序、错误 ID、空回复、合法更新块及诊断持久化脱敏；当前源码专项测试、双类型检查、MVU Prompt 测试及隔离产物中的角色运行时／扩展包测试通过。
- 使用隔离候选 `mvu-refusal-20260923-r1`（原 `dist` 未覆盖）；本地测试酒馆 HTTP 可读，先备份头像并比对哈希，再仅对 `魔法少女世界 1.0.3.png` 安装运行时及界面壳。安装后 API 读回运行时 SHA-256 `3aef5cc45d039a9d0b899b5676f9f4969fd97f5c1010f5799b07d6ac99233260` 与隔离构建完全一致；其余 359 个受保护的角色、聊天、世界书、设置及扩展文件的路径与哈希不变。头像原件备份与安装恢复文件仅留在本机 `tmp/`。
- 未自动刷新酒馆、运行真实模型、推进聊天或改写存档。此更新**改进失败归因，不保证原请求能生成**；需要将未成年角色保持完全非性化或改用明确成年角色，删除伪系统覆盖指令后再由用户自行测试。未进行浏览器视觉和真实模型验收。
