# 1.0.2 发布记录

用户明确授权“发布最新版本”，由主代理直接完成，无新增子代理。

## 交付范围

- 角色卡显示名 `魔法少女世界 1.0.2`；世界书 `魔法少女世界1.0.2`；运行时 ID `magic-girl-world-runtime-1-0-2`；设计辅助器及最低兼容版本 `1.0.2`。
- 修复默认单次开局的卡牌引用展开、无 SubtleCrypto 时保存摘要、初始化与重试期间的剧情/状态栏隐藏、默认入口原回复留证。具体根因见前一份修复记录。
- 发布门禁发现并修复：括号/反向操作数/否定及条件数值中的姿态中文名称丢失；共享模型写作协议缺少已经支持的空过、台词与敌人意图说明。
- 将延迟创建的事件/开局遗物结算状态标记为程序拥有的可选字段，不更改初始化数据和存档。
- 完整更新条目唯一来源 `docs/player-release-notes.md`；论坛快照 `docs/forum-update-1.0.2.md`。

## 验证

- `prerelease:tavern` 与 `release:tavern` 构建前检查展开、去重后，加本次开局/摘要/浏览器/扩展专项，共177项全部通过。旧失败项按已存在的行动时机、保护顺序、三个地图入口、必选取消回滚、卡牌引用详情与奖励数量等契约修正后复核；没有移除对应执行断言。
- app 与 extension 类型检查、portable构建及消费声明检查通过。
- UI、扩展构建、扩展激活/隔离/失败恢复、角色运行时导出、PNG写入与卡内契约通过。PNG包含7条正则、MVU脚本和版本化角色运行时。
- 初始控制器回归涵盖引用解析、保存/恢复、不重复生成、奖励领取、实际伤害和双说明链；非法引用不写入候选。SHA-256覆盖标准向量、中文、边界长度和百万字节。
- 初始化可见性使用生产HTML/SCSS/渲染器在隔离浏览器390/1000宽度验证14种状态，未操作玩家页面。
- 隔离构建目录 `tmp/release102-workspace`，1002份源码哈希与主工作区一致；正式产物220份逐个备份、复制、复核哈希，备份在 `tmp/release102-backup`。
- 门禁明细 `tmp/release102-verification-summary.json`、源码绑定 `tmp/release102-source-binding.json`、产物绑定 `tmp/release102-promoted.json`，完整日志在同级 `release102-*` 文件中。
- PNG SHA-256：`3f10a1e3db47f0a1d9fcc64121ed0c338776d3f2cbec7790096da1f169be0577`。
- 扩展ZIP SHA-256：`f1491d3f68a875078a7345aa27c942aae132e9ec1457c75e32211cd26a136c56`。

## 发布位置与边界

主仓库分支 `codex/tower-mode`，扩展分支 `extension`；卡图仓库为 `D:/project/oldproject/card-upstream`，只同步 `cards/魔法少女世界.png` 与 `cards/魔法少女世界版本更新记录.md`。主仓库包含本版构建所需的既有未提交生产源码，避免只提交产物导致无法重建；与本版无关的临时导出、旧实验产物、研究和任务草稿留在原工作区。

本轮没有自动刷新玩家页面、写入或推进存档，也没有真实模型生成或现场实玩验收。controller既有ESLint问题保留，其基线见前一份修复记录；本次177项发布门禁不包含全仓ESLint，不宣称全仓零告警。
