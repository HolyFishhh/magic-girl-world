# 杀戮尖塔 2 机制缺口收尾（2026-09-23）

## 边界与实现

前一阶段 `f9fe60f` 已完成状态指定选择、复制和转移。本阶段完成剩余的卡牌额外／替代费用与通用结算前拦截；不把已有的敌方护卫分摊、召唤物拦截或卡牌权限重复计为新机制。

- 卡牌根部 `payment.additional` 与 `payment.alternatives`：通常费用与生命、其他手牌弃置、己方召唤物献祭组合；替代方案完整替换通常费用。可支付性在执行前基于当前资源、生命、实例重新核对；取消及失效选择不消费。生命至少保留 1 点，免费仅豁免资源费用，重放不重复付费，自动打出计入出牌规则窗口且事务失败回滚。
- 状态根部 `intercepts`：`before_damage` 在格挡前处理待结算伤害，`before_card_play` 在支付后、效果前处理牌；允许按条件、优先级、每回合／每场次数及消耗层数执行取消、伤害改写或有限替换。持有者身份、召唤物身份、保护溢出及取消后事件记录各自保持边界；保存后次数语义不漂移。
- 同步公开世界书／主提示、两份 Schema、DSL 编译／校验／修复字段白名单、公式和事件投影、运行时、存档恢复、中文两条说明链、战斗选择器和牌面说明。流派目录涵盖状态操作，防止出现协议支持但引导遗漏。

## 验证

- `npm run test:card-payment-contract`：合法方案编译与 Schema、精确费用和失效选择、费用事件、取消／效果失败回滚、免费及重放、存档等价、两条中文说明。
- `npm run test:interception-contract`：合法与非法结构、优先级及事件、取消／替换、持有者身份、伤害/出牌窗口、次数与层数、JSON 恢复、事务回滚。
- `npm run test:auto-play-runtime`：自动打出事务夹具接入真实快照，规则窗口仅计一次，重放不重复计数。
- 隔离浏览器 390px / 1000px：费用方案、弃牌／献祭实例、状态选择及取消；人工查看 390px 的 `payment-390.png`、`discard-390.png`、`status-390.png`，页脚可见，长内容换行，弃牌列表滚动。证据在本机 `tmp/mechanism-choices-visual/`。

完整机制回归在前半段自动出牌夹具（缺事务宿主）和后段状态触发旧三参断言处分别中断；已修正夹具并单独通过，后半段继续运行至结尾，`test:sts2-mechanics` 和 32 组稳定性通过。公开操作矩阵为 69 个操作／114 个分支；应用与扩展双类型检查、世界书契约、流派覆盖、多敌集成和牌事务通过。

这些是自动化、离线／隔离浏览器证据；未调用真实模型，没有刷新或推进玩家当前聊天，也不宣称完整玩家实玩验收。

## 构建和本地测试酒馆

源码提交 `acc7e80`；隔离构建候选 `mechanisms-20260923-r1`，构建前后原 `dist` 与已安装扩展共 1031 个文件哈希不变（源码冻结 1035 个文件）。扩展包及角色运行时产物检查通过，候选未覆盖旧产物。

已安装到本地测试酒馆 `http://127.0.0.1:8012/`，仅更新现有最新测试角色 **魔法少女世界 1.0.3**（精确头像 **`魔法少女世界 1.0.3.png`**）的运行时与四个 UI 壳、测试扩展的 5 个文件，以及对应世界书 ID 5「当前状态与构筑」和 ID 1「通用效果 DSL」的正文；旧版本角色卡不覆盖。角色运行时 API 回读 SHA-256 `9cf1498979ee972f09cd75a3b1f5b21ed8ed7c9398cb8f74d406fe033b7d8605`；扩展入口 HTTP SHA-256 `bfd020f885f78b09c978fd4a8c19b04119a4dc6ce8e1cd2863e52077a65f25a6`，其余 4 项扩展 HTTP 哈希也与候选一致。两个世界书载体回读一致；安装脚本逐次确认选中聊天、其他角色字段及相关设置不变。未刷新酒馆页面，也未生成内容或推进存档；用户须自行刷新完整页面以加载新运行时。

可逆备份：`tmp/tavern-extension-backups/2026-09-23T03-13-17.937Z-634d2975-0e8c-46f6-b860-85683ac1a3c6/`、`tmp/tavern-runtime-backups/64a794b0-590a-4073-9a8b-d8e487cdb9dd/`、`tmp/tavern-worldbook-backups/bd8b1650-60c4-4059-b7f2-387465ef17f4/`；绑定与安装核验见 `tmp/build-binding-mechanisms-20260923-r1.json`、`tmp/mechanisms-*-install-20260923.json`、`tmp/verify-mechanisms-install.mjs`。这些本机备份和玩家数据不提交 Git。
