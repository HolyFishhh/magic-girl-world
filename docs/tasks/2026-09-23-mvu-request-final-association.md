# 2026-09-23 刷新后的 MVU 失败：区分请求与完成输出

## 现场证据（仅只读）

- 用户本次消息仍指向旧附件 `mwg-generation-diagnostic-1790144395958.json`；下载目录另外已有新导出 `mwg-generation-diagnostic-1790146365818.json`，时间为北京时间 14:52:45。仅从新文件提取时间、字段、状态与结构标记，不提交剧情、请求正文或诊断原件。
- 刷新后曾于 14:27:42–14:28:44、14:30:47–14:32:23 两次完成变量应用。下一轮 14:49:44.577 开始，14:51:31.118 报错：MVU 楼层解析的 3180 字符不含 `<UpdateVariable>`。该文本被标记为 `mvu-command-parsed` 的回退楼层，**不能称为提供方原始响应**；`originalResponse.availability=missing`。
- 同一轮 14:49:44.616、14:49:58.206 有两次“捕获实际模型请求”；14:49:58.059 记录的 1858 字符 `mvuHelperFinal` 早于第二次请求，不能据此推断后一次模型输出没有更新块。后一次请求审计有 14 条消息、约 91134 字符及 MVU 更新协议标记；旧版审计 `hasCurrentMvuFacts=false` 也不能直接证明真正发送的提示词缺少事实，因为旧实现优先审计 `messages`，注入器却优先修改 `prompt`。这两条必须分开核验。
- 测试酒馆 `127.0.0.1:8012` 现场仍监听；新文件包含刷新后运行时新增的诊断字段，不能继续把本次归因于“旧页面”或事后无服务。现有证据也**不足以证明提供方拒绝、模型格式跑偏、MVU 丢包或 Prompt 注入失败中的哪一种**。不自动生成或改动存档。

## 修订与边界

- 当活动 MVU 轮次中捕获了不同的新请求，清除上一请求的助手完成快照，排除已被取代的 generation ID，并允许后一次请求绑定自己的完成输出；同一请求的重复兼容事件仍保留完成快照。MVU 生命周期标志先降下时，已经观察到的活动请求仍可接收相应的 Tavern Helper 完成事件。整个机制仅改变诊断关联，不自动重试、伪造更新或写玩家变量。
- 请求摘要按真实提示词注入器的优先级选择 `prompt/messages/chat`，附无正文的 `requestField` 和 `hasDesignContextMarker`，使“当前 MVU 游戏事实”检测和实际修改的载体一致。仍不能冒充提供方网络层原始响应。
- 回归覆盖先有生成完成、再捕获下一 MVU 请求、旧 generation 晚到、同一请求重复事件、生命周期标志提前下降和双数组载体的摘要；不以测试冒充玩家实玩或真实模型验收。

## 验证与本地安装

- 两套 TypeScript 检查；当前源码的 `test-mvu-monitor-chat-scope`（含本轮跨请求、晚到事件和双载体回归）、`test:mvu-prompt-context`、扩展控制器测试、隔离产物的 `test-tavern-character-runtime` 均通过。普通项目目录的旧 `dist` 当时仍是 1.0.2，直接运行产物契约会因版本不符失败；改用隔离构建并验证 1.0.3，而非覆盖既有 `dist`。
- 隔离产物 `tmp/runtime-build-mvu-request-correlation-20260923-r1` 构建／导出成功；运行时 SHA-256 **`f768f03e5b7d87c4563e391516c41be9f169724f7ae7bd0557813ed8e397f09e`**。构建前后 1034 个受保护的当前源码／原 `dist`／现有扩展／选中聊天文件哈希不变。`tmp/` 内基线与构建目录仅作为本机证据，不纳入 Git。
- `127.0.0.1:8012` 返回 HTTP 200；先对准确头像 **`魔法少女世界 1.0.3.png`** 的 PNG 做原件备份与 SHA-256 核验，再使用只合并运行时和四份界面壳的本地安装脚本安装。API 读回显示 **魔法少女世界 1.0.3**、版本 1.0.3，安装版运行时内容与隔离产物逐字一致；其他 360 个角色／聊天／世界书／设置文件哈希不变，安装脚本复核选中聊天、全局正则和其他角色字段未变。安装脚本的可逆备份在 `tmp/tavern-runtime-backups/daa74244-67d2-4d70-a6b0-5e15cfd4246f`，额外 PNG 原件备份及保护基线在 `tmp/`。
- 本次**没有修改世界书和顶层扩展，也没有刷新浏览器、自动生成或推进存档**；玩家当前页面仍可能执行旧内存脚本。用户需自行完整刷新页面后再实测并导出新的报告。**真实模型失败根因尚未确定、游戏生成尚未宣称修复**；本轮完成的是避免错误归因并让下一次诊断真实关联请求和完成输出。
