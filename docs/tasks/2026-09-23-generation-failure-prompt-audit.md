# 2026-09-23 连续生成失败与世界书 Prompt 核查

## 交付状态

已修订源码并完成隔离构建，**未安装到本地测试酒馆，未完成真实模型生成或实玩验收**。本轮仅修复已证实的 Prompt 矛盾与诊断来源误标；两次生成失败的上游根因仍缺少请求/响应证据，不能宣称失败已彻底解决。

待安装候选：`mvu-prompt-audit-20260923-r1`。目标显示名 **魔法少女世界 1.0.3**，精确头像文件 **魔法少女世界 1.0.3.png**，关联世界书 **魔法少女世界1.0.3**。本轮没有推送或正式发布，没有刷新页面、调用模型、重试生成、推进聊天、领取奖励或改写玩家存档。

## 两份诊断能证明什么

输入文件（只读，不提交附件或剧情原文）：

- `C:/Users/holyf/Downloads/mwg-generation-diagnostic-1790135748899.json`
- `C:/Users/holyf/Downloads/mwg-generation-diagnostic-1790135814766.json`

两次均为同一剧情聊天的 MVU 第二阶段错误：未得到可解析的 `<UpdateVariable>` 更新块；运行约 23.43 秒和 23.53 秒。导出均声明 `originalResponse.availability: missing`，只有 `matching-live-monitor-output-fallback`（2908 / 2579 字符），没有可确认的提供方原始返回。当前保存数据是 story 开局，仍有初始化等待标记；不能把这次日志当成前一任务的第二幕馈赠故障。

第二份回退文本与保存的 assistant 剧情楼层相同。当前 MVU 的 `COMMAND_PARSED` 可收到整个楼层；额外模型生成失败时，该楼层可能仍是未修改的剧情。所以旧文案“第二轮模型没有返回”以及“模型返回完成”把缺失更新块的现象过早归因给了模型。不能据此断言第二轮在续写剧情，也不能认定灵巧、遗忘或预设状态导致了这两次失败。

已核查本机配置为 MVU 内置预设、额外模型解析、`[mvu_update]` 白名单与串行重试。**配置存在不等于指令确实进入了这两次请求**。没有保留这两次完整请求及提供方响应，无法在 Prompt 装配、模型格式输出、接口适配/传输之间进一步定责；未使用不相关旧服务日志填补证据空缺。所有密钥与配置正文均未进入提交。

## 世界书核查及修订

从本地测试酒馆磁盘只读核对：`D:/project/oldproject/_codex-tavern-data-tower-e2e/default-user` 中最新卡和关联世界书。七个相关条目均已启用，安装正文与本轮前的 HEAD（`1996408`）对应源码一致。

| 机制 | 修订前已写入并安装 | 本轮处理 |
| --- | --- | --- |
| 灵巧 | `sly:true` 已在通用 DSL 中说明 | 保留既有执行语义，增加文档存在性回归 |
| 遗忘 | `lifecycle.on_discard:"purge"` 已说明，公开名称为“遗忘” | 保留生命周期和权限语义，验证现有编译/执行/保存与显示回归 |
| 预设状态 | 15 项 `sts_` 精确 ID、中文规则与直接引用方法已存在 | 清理七份世界书“程序没有内置状态”的冲突规则；移除重复旧目录，保留准确荆棘说明 |

预设白名单包括力量、敏捷、虚弱、易伤、脆弱、中毒、再生、金属化、仪式、荆棘、壁垒、活力、人工制品、无实体、缓冲对应的 15 个 ID，准确执行定义以 `src/game-core/builtinStatusCatalog.ts` 为准。

新增共享 `builtinStatusReferenceContract()`，由可执行目录导出精确白名单。同步七份世界书、爬塔请求、初始内容/修复提示与战斗节点 Schema 描述：仅白名单 ID 可以免写定义并按真实引用展开依赖、保存；显式同 ID 定义优先；其他状态仍需完整注册；任意 `sts_` 前缀或中文名称不是内置机制。核心剧情状态不能随意替换成通用预设。本轮没有新增执行枚举、修改存档结构或增加内容质量硬门槛。

对应待安装世界书 ID：**0、1、2、6、9、10、17**。只读核查摘要保存在 `tmp/mvu-worldbook-audit-20260923.json`，原卡/世界书审计快照仅本机保留，不提交。

## 诊断改进与边界

- 解析事件改标为“MVU 楼层解析完成”，UI 明确“非模型原始响应”，回退来源为 `mvu-command-parsed`。
- 缺失更新块仍判为错误，不伪造成功、不改写或吞掉玩家数据；提示改为已观察到的事实，而非直接归因模型。
- 增加无正文请求摘要：消息角色、数量、字符数及固定协议标记检测。未命中不等于没有等义指令，捕获到也不证明提供方收到了这些指令。
- 单独观察 `js_generation_ended` 助手完成文本，保留空返回与未观察到完成事件的区别。按活动普通 MVU、请求已捕获、生成 ID / attempt ID 做有限关联；临时 ID 绑定首个流式或完成事件，**不是网络级请求关联证明**，也不能保证覆盖每次重试。
- 助手输出与解析楼层分别导出，不冒充 provider 原始响应；去掉既有支持的内嵌思考标签并做密钥脱敏，不导出请求正文。新文本证据只存在当前页面内存，不写入轻量 localStorage，切换聊天或新轮次清理。

## 验证

通过：

- 项目 app 与扩展两套 TypeScript 检查（`tsconfig.app.json` / `tsconfig.sillytavern-extension.json`）。
- `test-mvu-monitor-chat-scope`：原剧情回显/助手输出分离、请求标记、空完成、错误 ID、换聊天/新轮次隔离、脱敏及不持久化正文。
- `test-builtin-status-integration`：初始草稿编译、奖励领取、敌人激活、存档恢复及中文显示；七份世界书白名单一致、目录不重复、灵巧/遗忘说明存在、两种创作协议及三种战斗 Schema 共用白名单、修复提示边界。
- `test-builtin-statuses`、`test-ability-trigger-integration`、`test-card-lifecycle`、`test-card-removal-permissions`。
- `test-worldbook-contract`、`test-tower-single-floor-start`、`test-mvu-prompt-context`、`test-mvu-request-policy`、`test-diagnostic-redaction`、`test-sillytavern-design-assistant-plugin`。
- 隔离扩展及运行时构建、导出、扩展包检查、编译后 `test-tavern-character-runtime`；1035 个源文件冻结哈希一致，原 dist/已安装扩展共 1031 个保护文件未变。
- 本任务差异 `git diff --check`。

**已知既存失败**：`test-tower-request.mjs` 第 88 行仍期待旧提示 `cost:"energy" 只会支付能量`。使用 HEAD 版本的测试及本轮修改的两个被测模块隔离复现同一失败（`tmp/check-baseline-tower-request.mjs`），没有回滚工作树或删测试绕过；本轮仅更新其中过期的“没有内置状态”断言，不将整套测试报告为通过。

最初非项目标准入口 `tsc --noEmit` 遍历范围过大，发生 Node 4GB 堆内存不足；随后按项目正式两套配置检查通过。没有进行本轮真实浏览器视觉、真实模型或玩家存档实玩验收；离线回归不等于线上故障复现。

构建绑定：`tmp/build-binding-mvu-prompt-audit-20260923-r1.json`。

- 扩展 SHA-256：`92094f1588fb1cefa3a2bdfa8608229f31629d6fa92f52b452945e78f3753143`
- 运行时 SHA-256：`3a37227f081f37688d22f1467b7bcd37d4816344a6dc063b21cb11e9a44d4bb9`

## 安装与后续取证

本地测试酒馆 `127.0.0.1:8012` 多次只读探测均为 `ECONNREFUSED`，未擅自启动服务或手工覆盖磁盘卡片。**在本任务源码验收时，已安装入口仍为本轮前版本；后续安装结果见本节末尾接续。**

待酒馆启动后，先重新核对冻结哈希与安装基线，按现有安装工具备份安装扩展、最新 1.0.3 卡内运行时及上述七个世界书条目，并核验 API/HTTP 回读、旧卡和聊天保护。不要覆盖旧版卡，不自动刷新、重生成或改写玩家存档。安装成功后由用户自行刷新并手动发起下一次请求；若仍失败，立即导出新的排查记录，结合请求标记、助手完成事件与楼层解析分别判断，再决定是否需要更底层取证。

**2026-09-23 安装接续：**上述“未安装”是本任务源码验证时的状态。随后使用包含本任务修订与 15 项预设使用范围修订的最新隔离候选 `preset-scope-20260923-r1` 完成 8012 测试酒馆安装；扩展哈希应以该候选的 `4988740b59e0832b79076edd75da63bcc56ecca3191225c6195eddc61b48852d` 为准，而不是上列未安装的单轮候选。卡内运行时、扩展五文件及七个世界书条目均已备份和 API/HTTP 回读；未刷新页面或请求真实模型。详见 [安装接续](2026-09-23-preset-status-authoring-scope.md)。
