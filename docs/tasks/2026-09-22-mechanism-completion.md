# 机制补完与验证（2026-09-22）

## 第一阶段：卡牌永久移除和变形权限

卡牌及临时模板的 `lifecycle` 新增两个独立可选布尔字段：`removable:false`（不可永久移除）和 `transformable:false`（不可变形）。省略即允许，`unique` 仍仅限制额外副本。不可移除只保护长期牌组：消耗、遗弃和战斗牌区移除照常；遗忘/打出销毁仍退出本场，但原持有卡战后保留。若还要防止通过变形换掉原卡，须同时设置不可变形。

底层持有卡事务统一拒绝受保护实例的永久移除，覆盖删卡次数、商店、营火及事件；失败不扣次数、金币或修改事务版本。对应选择器过滤不可操作实例；所有牌受保护时不弹空删牌窗。战斗内变形同时在候选与实际提交处检查；永久销毁同时检查战斗实例和原持有实例，避免临时变形绕开原卡限制。

公开卡牌协议、两份 JSON Schema、共享生命周期校验、模板编译/投影、MVU 转换、持有卡序列化、战后恢复、统一词条以及两条中文说明链同步。嵌套字段仍沿用 `lifecycle` 的既有根字段白名单和修复 Schema 来源，无新增旧格式迁移。

回归 `test-card-removal-permissions.mjs` 覆盖公开卡牌及 AST Schema、合法编译/非法值、两条说明、精确实例删除拒绝、事件候选、空候选不提交、真实弃牌与回合清理、临时变形绕过防护、JSON 保存恢复及战后结算；另验证商店拒绝不扣金币/不占服务次数。既有取消回归保持通过。

复查还补齐馈赠批量变形：执行和预览共同跳过不可变形卡。永久删除 API 返回实际结果，战斗日志及动画区分“永久销毁”与“移出本场、原持有牌组保留”。流派/评分风险证据同样区分受保护的遗忘与真正永久丢牌，避免错误报告长期风险。

最终验证：29 组稳定性回归、5 组额外定向检查（批量转化、牌区事务、战外事务、待删卡队列、紧凑 DSL）及应用/扩展双类型检查通过。最后增加的评分证据分支又通过权限契约、评分准确性与应用类型检查。390/1000 像素隔离 Edge 商店检查通过，覆盖受保护牌过滤、取消、确认、缩放及父页面滚动；已查看 390 像素截图，按钮和弹层均在可见区域。该浏览器夹具使用简化卡面，不代表完整玩家实玩验收；没有调用真实模型。

## 继续实施

状态按类型整批净化已经存在（`remove_status:"buffs"/"debuffs"/"all"`）；上一轮缺口描述过宽，真正需要补的是指定数量的交互选择和状态转移/复制。额外/替代费用以及通用结算前拦截仍需分别完成完整协议和事务回归。

## 本地交付

源码提交 `bfd94b7`、`d12a2ba`；候选 `permissions-20260922-r1`。1028 份源码冻结哈希一致，隔离构建期间 1031 份 dist/安装目录文件未变；扩展/UI 构建、导出、包及角色运行时检查通过。安装后仅强化回归脚本，让自动弃牌走真实 `discardHand()`，已验证相同测试正文；没有再次修改运行时代码。

已安装到 `http://127.0.0.1:8012/`；角色显示名 **魔法少女世界 1.0.3**，精确头像文件名 **魔法少女世界 1.0.3.png**。卡内运行时回读、安装前后选中聊天/其他角色信息/全局正则保护和 5 项扩展 HTTP 哈希通过。没有刷新页面、生成内容、领取奖励或推进玩家存档。用户自行刷新完整酒馆页以加载新运行时。

- 运行时 SHA-256：`adc152c10a2bb115cd2588bea7b6b7bf572a5bfdb1de92dcf87b1f1127a56ae2`。
- 扩展入口 SHA-256：`6b54d3efc2e30192ae7edc28af9301204bb0f5bc0029ec0411a6bc43c9376fc1`。
- 运行时备份：`tmp/tavern-runtime-backups/836b798a-0f04-4ac6-9657-74d39595e8b4/`。
- 扩展备份：`tmp/tavern-extension-backups/2026-09-22T09-25-02.792Z-2dc66034-0fd9-4409-9309-0547b15d70a4/`。
- 证据：`tmp/permissions-verification.json`、`tmp/build-binding-permissions-20260922-r1.json`、`tmp/permissions-install-final.json`、`tmp/permissions-data-diff.json`、`tmp/shop-fixed-visual/evidence/`。

全目录保护检查有一项差异，不能记作全部通过：351 份基线文件中，350 份未变，另一个角色的一份聊天记录在 09:20:15 UTC 更新；扩展安装备份始于 09:25:02 UTC，角色备份始于 09:25:04 UTC，变化早于本轮安装约 5 分钟。没有增删文件，未回滚或改写该聊天；差异保留在本机证据中。该差异与已通过的安装前后选中聊天检查分开报告，未重新设基线来掩盖变化。
