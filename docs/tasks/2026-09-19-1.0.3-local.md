# 1.0.3 本地版本更新

用户在本地测试安装后要求直接更新1.0.3。本轮输出并安装独立版本的新卡/世界书/运行时及扩展，保留旧卡与存档，不推断公开推送授权。

## 源码与清理
- 纳入82b1043剧情状态与战后剧情，以及968b8ee生成队列、切换与窄屏稳定性修复。
- 纳入现有挂载epoch保护源码及其回归：它是已提交runtimeViewSwitcher依赖的构建输入，不能只发布未入Git的依赖。
- 当前入口使用story/tower及锁，清理expedition别名与按旧run猜测模式的迁移；旧模式拒绝并保留原数据。保留当前初始化及镜像同步、当前协议解析/有限修复和模型/浏览器适配。
- 移除旧手动扩展自动删除后重装的迁移分支，提示手动更新，保留官方仓库扩展更新入口。
- 界面壳仅去除生成代码行首缩进，从6638降至约5434字符，保留6000门槛及加载/恢复/切换回归。
- Luna限流、Terra无可用通道，均未产生修改，主代理完成。

构建、验证及安装结果在完成后补充；未进行真实模型验收。

## 验证阶段
- 182项去重门禁完成。首次失败两项：common-interface旧函数名断言，以及runtimeFrameHeight在父级清空style.height后被lastHeight防抖误拦。前者同步当前入口，后者修复并增加一像素抖动保持回归；原始失败与复查证据均保留，182项最终通过。
- app/extension类型、portable构建与消费声明、UI/extension生产构建、runtime与shell导出、完整runtime测试、扩展ZIP打包检查、角色卡内契约通过。最终高度补丁已重建runtime与卡并通过完整runtime和卡内契约。1007份构建输入与主工作区哈希一致。
- 新卡最低扩展版本及能力报告同步1.0.3，修正原来介绍中的1.0/0.3.5文案漂移。
- 当前源码快照在tmp/release103-workspace；原有无关文档与dist杂项不纳入本任务源码提交。

## 本地交付完成
- 源码提交2e9b792，独立角色魔法少女世界 1.0.3.png已通过官方本地导入API安装并选中；新世界书魔法少女世界1.0.3已安装，含22条条目、7条正则、MVU及版本化运行时两个角色脚本，作用域权限已启用。
- 扩展1.0.3备份安装完成，5份产物HTTP回读SHA256一致。337个原有角色/世界书/聊天文件哈希不变，设置仅变更新角色选择及其脚本/正则白名单。旧卡与存档保留。
- 主卡PNG与dist/release下1.0.3卡图/扩展ZIP已保存，原主卡有本机备份；没有覆盖其他既有dist工作树产物。
- 安装证据tmp/final-verification-release103-local.json，源码/产物绑定tmp/release103-final-source-binding.json与tmp/release103-artifact-binding.json。
- 页面未自动刷新，未进行真实模型生成、推进存档或远端推送。用户刷新整个酒馆后使用新卡新开局即可测试。全部182项离线/隔离检查通过不等于真实模型实玩验收。
