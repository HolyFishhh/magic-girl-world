# 论坛版本更新记录：beta0.5.88

- beta0.5.88：
  - 修复 MVU 额外模型解析输出嵌套 XML 时整块变量更新失败；变量轮次现在明确只接受 `_.set`、`_.assign`、`_.remove`、`_.add`。
  - 新增轻量 `[mvu_update]` 格式约束，复杂卡牌和敌人对象仍使用 JSON + 简易公式，不增加第二套卡牌语法。
  - 角色显示名改为 `魔法少女世界 0.5.88`，内嵌世界书改为 `魔法少女世界0.5.88`，避免酒馆重复导入旧同名卡时复用脚本或世界书状态。
  - 继续保持单一根目录角色卡交付，正文原生显示，状态栏在消息末尾渲染，战斗页面仍在引导正文之后。
