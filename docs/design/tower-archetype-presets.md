# 爬塔流派预设目录

<!-- Generated by node scripts/export-tower-archetype-catalog.mjs; edit src/game-core/towerArchetypeCatalog.ts. -->

当前收录 **12 个类别、144 项基础机制**，覆盖现有流派图谱的 **67 个节点**。

这是一份供玩家多选组合的基础机制目录。具体题材、外观、卡名和故事仍由角色及世界决定；目录不预设跨机制的捆绑获胜路线，也不限制自由填写的构筑。

## 使用与维护

- 两个爬塔开局入口共用此目录。可跨类别多选，搜索持续保留已选项；已选菜单可以逐项移除或清空。
- 选择只更新现有卡组要求中的机制段落；浏览与筛选不会修改输入或启动游戏。别名只用于识别旧存档，不会进入生成 Prompt。
- 清空保留机制段落外的自定义文字。旧版单选预设会以可见的旧版提示保留，玩家可另选基础机制；命名开局预设保存完整原文。
- 维护数据源 `src/game-core/towerArchetypeCatalog.ts`：保持稳定 ID，填写名称、通俗说明、一个可独立选择的机制关注点、图谱关联与操作关联。
- 二级玩法改名时把旧名称保留为别名，确保旧开局预设仍能识别；别名不可与其他玩法名称冲突。变更机制含义时新增 ID，避免将已有身份改作其他用途。
- 新机制落地时，补充可独立选择的基础关注点或修订现有要求；不要把多个成熟机制包装成新的预制组合。分类只表示浏览位置，机制可关联多个图谱节点。
- 图谱覆盖与操作审计属于目录维护检查，不是玩家卡组的准入门槛。程序不会根据预设强制改牌、限制数量或拒绝合法构筑。
- 更新后运行 `node scripts/export-tower-archetype-catalog.mjs` 与 `node scripts/test-tower-archetype-catalog.mjs`，并验证选择、切换、清除、保存与载入。目录快照由同一数据源生成。

## 伤害与终结

选择伤害结构、倍率条件与终结方式。

### 即时伤害

直接打出伤害，快速压低敌方生命。

**基础关注点：** 即时伤害

- 直接打出伤害，快速压低敌方生命。

维护 ID：`direct-damage`；图谱：`direct-pressure`；操作：`damage`。

### 多段伤害

把伤害分成多次命中，配合每次命中触发的能力。

**基础关注点：** 多段伤害

- 把伤害分成多次命中，配合每次命中触发的能力。

维护 ID：`multi-hit`；图谱：`multi-hit`；操作：`damage`。

### 伤害倍率

先挂上倍率修饰，再让指定伤害获得更高收益。

**基础关注点：** 伤害倍率

- 先挂上倍率修饰，再让指定伤害获得更高收益。

维护 ID：`damage-amplification`；图谱：`critical-scaling`；操作：`modify`。

### 穿透格挡

让伤害绕过格挡，直接削减敌方生命。

**基础关注点：** 穿透格挡

- 让伤害绕过格挡，直接削减敌方生命。

维护 ID：`piercing`；图谱：`direct-pressure`；操作：`damage`。

### 阈值处决

先把敌人压到阈值，再用处决收尾。

**基础关注点：** 阈值处决

- 先把敌人压到阈值，再用处决收尾。

维护 ID：`execute`；图谱：`execute-finish`；操作：`execute`。

### 直接终结

满足明确条件后直接击杀目标，换取快速终结。

**基础关注点：** 直接终结

- 满足明确条件后直接击杀目标，换取快速终结。

维护 ID：`fatal-kill`；图谱：`execute-finish`；操作：`kill`。

### 已损生命计值

读取已损生命来提高效果，越受伤收益越明显。

**基础关注点：** 已损生命计值

- 读取已损生命来提高效果，越受伤收益越明显。

维护 ID：`missing-hp`；图谱：`missing-hp-pressure`；操作：。

### 格挡计值

读取当前格挡来放大效果，把防守转成收益。

**基础关注点：** 格挡计值

- 读取当前格挡来放大效果，把防守转成收益。

维护 ID：`block-value`；图谱：`block-conversion`；操作：。

### 状态层数计值

读取状态层数来提高效果，把叠层转成收益。

**基础关注点：** 状态层数计值

- 读取状态层数来提高效果，把叠层转成收益。

维护 ID：`status-value`；图谱：`status-conversion`、`status-scaling`；操作：。

### 资源计值

读取资源数量或支付量，把储备转成一次收益。

**基础关注点：** 资源计值

- 读取资源数量或支付量，把储备转成一次收益。

维护 ID：`resource-value`；图谱：`resource-cashout`；操作：。

## 状态与持续能力

选择状态生命周期、叠层与独立能力。

### 状态叠层

反复施加同一状态，靠层数积累后续收益。

**基础关注点：** 状态叠层

- 反复施加同一状态，靠层数积累后续收益。

维护 ID：`status-stack`；图谱：`status-stack`；操作：`apply_status`。

### 限时状态

施加带期限的状态，利用短时间窗口完成收益。

**基础关注点：** 限时状态

- 施加带期限的状态，利用短时间窗口完成收益。

维护 ID：`timed-status`；图谱：`status-stack`；操作：`apply_status`。

### 状态净化

移除指定状态或层数，清除不利状态或配合消耗收益。

**基础关注点：** 状态净化

- 移除指定状态或层数，清除不利状态或配合消耗收益。

维护 ID：`status-cleanse`；图谱：`status-detonation`；操作：`remove_status`。

### 状态复制

从符合筛选的状态中选择，复制给目标并保留状态身份与期限。

**基础关注点：** 状态复制

- 从符合筛选的状态中选择，复制给目标并保留状态身份与期限。

维护 ID：`status-copy`；图谱：`status-stack`；操作：`status_action`。

### 状态转移

从符合筛选的状态中选择，转移给目标并保留状态身份与期限。

**基础关注点：** 状态转移

- 从符合筛选的状态中选择，转移给目标并保留状态身份与期限。

维护 ID：`status-transfer`；图谱：`status-stack`；操作：`status_action`。

### 持续伤害

让状态按回合重复造成伤害，持续消耗敌方生命。

**基础关注点：** 持续伤害

- 让状态按回合重复造成伤害，持续消耗敌方生命。

维护 ID：`periodic-damage`；图谱：`damage-over-time`；操作：`apply_status`、`damage`。

### 形态联动

进入指定形态，让关联卡牌变强；不必靠反复叠层。

**基础关注点：** 形态联动

- 进入指定形态，让关联卡牌变强；不必靠反复叠层。

维护 ID：`status-form-engine`；图谱：`status-form-engine`；操作：`apply_status`、`modify`。

### 状态条件

先检查状态存在或层数，再决定效果是否生效。

**基础关注点：** 状态条件

- 先检查状态存在或层数，再决定效果是否生效。

维护 ID：`status-gate`；图谱：`status-scaling`；操作：。

### 状态消耗

消耗已有状态层数，换取一次明确效果。

**基础关注点：** 状态消耗

- 消耗已有状态层数，换取一次明确效果。

维护 ID：`status-consumption`；图谱：`status-detonation`；操作：`remove_status`。

### 敌方状态响应

给敌方施加或观察状态，在对应事件发生时触发收益。

**基础关注点：** 敌方状态响应

- 给敌方施加或观察状态，在对应事件发生时触发收益。

维护 ID：`enemy-status-event`；图谱：`enemy-status-benefit`；操作：。

### 持续能力

部署一项持续能力，让指定事件反复带来效果。

**基础关注点：** 持续能力

- 部署一项持续能力，让指定事件反复带来效果。

维护 ID：`persistent-power`；图谱：`power-engine`；操作：。

### 持续属性修饰

在指定期限内修改属性，让后续效果持续变强或变弱。

**基础关注点：** 持续属性修饰

- 在指定期限内修改属性，让后续效果持续变强或变弱。

维护 ID：`stat-modifier`；图谱：`status-scaling`；操作：`modify`。

## 欲望

选择欲望压力、阈值和数值读取。

### 欲望施压

增加敌方欲望，用欲望压力推动战斗。

**基础关注点：** 欲望施压

- 增加敌方欲望，用欲望压力推动战斗。

维护 ID：`lust-pressure`；图谱：`desire-pressure`；操作：`lust`。

### 欲望降低

降低目标欲望，延缓危险并制造反击空间。

**基础关注点：** 欲望降低

- 降低目标欲望，延缓危险并制造反击空间。

维护 ID：`lust-reduction`；图谱：`desire-pressure`；操作：`lust`。

### 欲望重置

把欲望设为指定值，直接控制当前压力。

**基础关注点：** 欲望重置

- 把欲望设为指定值，直接控制当前压力。

维护 ID：`lust-reset`；图谱：`desire-pressure`；操作：`set_lust`。

### 欲望满溢

围绕欲望达到上限设计效果，抓住满溢时机兑现收益。

**基础关注点：** 欲望满溢

- 围绕欲望达到上限设计效果，抓住满溢时机兑现收益。

维护 ID：`lust-overflow`；图谱：`desire-overflow`；操作：。

### 欲望计值

读取当前欲望或比例，把欲望转成数值收益。

**基础关注点：** 欲望计值

- 读取当前欲望或比例，把欲望转成数值收益。

维护 ID：`lust-value`；图谱：`desire-conversion`；操作：。

### 欲望上限永久成长

永久提高欲望上限，让后续战斗拥有更大积累空间。

**基础关注点：** 欲望上限永久成长

- 永久提高欲望上限，让后续战斗拥有更大积累空间。

维护 ID：`lust-growth`；图谱：`desire-overflow`；操作：`persistent_growth`。

## 格挡与生命

分别选择防护、治疗和生命条件。

### 吸血生存

造成伤害后，按敌人实际损失的生命为自己回血。

**基础关注点：** 吸血生存

- 造成伤害后，按敌人实际损失的生命为自己回血。

维护 ID：`lifesteal`；图谱：`healing-conversion`；操作：`damage`。

### 获得格挡

获得格挡，先抵消伤害再争取行动空间。

**基础关注点：** 获得格挡

- 获得格挡，先抵消伤害再争取行动空间。

维护 ID：`block-gain`；图谱：`block-engine`；操作：`block`。

### 格挡留存

让格挡跨回合保留，把本回合防守延续到下回合。

**基础关注点：** 格挡留存

- 让格挡跨回合保留，把本回合防守延续到下回合。

维护 ID：`block-retain`；图谱：`block-retention`；操作：`card_rule`。

### 格挡重置

把格挡设为指定值，稳定控制防护规模。

**基础关注点：** 格挡重置

- 把格挡设为指定值，稳定控制防护规模。

维护 ID：`block-reset`；图谱：`block-engine`；操作：`set_block`。

### 生命恢复

治疗指定目标，恢复生命并延长作战时间。

**基础关注点：** 生命恢复

- 治疗指定目标，恢复生命并延长作战时间。

维护 ID：`healing`；图谱：`healing-engine`；操作：`heal`。

### 生命设值

把生命设为指定值，直接调整当前生存状态。

**基础关注点：** 生命设值

- 把生命设为指定值，直接调整当前生存状态。

维护 ID：`hp-reset`；图谱：`healing-engine`；操作：`set_hp`。

### 主动自伤

主动损失生命，换取更高输出或其他收益。

**基础关注点：** 主动自伤

- 主动损失生命，换取更高输出或其他收益。

维护 ID：`self-damage`；图谱：`self-damage`；操作：`damage`。

### 低生命条件

在低生命时提高效果，把危险状态转成战斗优势。

**基础关注点：** 低生命条件

- 在低生命时提高效果，把危险状态转成战斗优势。

维护 ID：`low-hp`；图谱：`missing-hp-pressure`；操作：。

### 满生命条件

保持满血来触发效果，奖励稳定防守。

**基础关注点：** 满生命条件

- 保持满血来触发效果，奖励稳定防守。

维护 ID：`full-hp`；图谱：`healing-engine`；操作：。

### 受击反击

先承受攻击，再按实际受击触发反击。

**基础关注点：** 受击反击

- 先承受攻击，再按实际受击触发反击。

维护 ID：`retaliation`；图谱：`retaliation`；操作：`damage`。

### 生命上限成长

提高最大生命，让后续战斗拥有更大的容错。

**基础关注点：** 生命上限成长

- 提高最大生命，让后续战斗拥有更大的容错。

维护 ID：`max-hp-growth`；图谱：`healing-engine`；操作：`persistent_growth`。

## 牌区与手牌

分别选择看牌、移动、回收与离场方式。

### 抽牌

抽更多牌，更快拿到本回合需要的组件。

**基础关注点：** 抽牌

- 抽更多牌，更快拿到本回合需要的组件。

维护 ID：`draw`；图谱：`draw-engine`；操作：`draw`。

### 占卜

查看并整理牌库顶部，改善下一次抽牌。

**基础关注点：** 占卜

- 查看并整理牌库顶部，改善下一次抽牌。

维护 ID：`scry`；图谱：`topdeck-control`；操作：`scry`。

### 定向检索

按条件检索指定卡牌，稳定拿到关键组件。

**基础关注点：** 定向检索

- 按条件检索指定卡牌，稳定拿到关键组件。

维护 ID：`seek`；图谱：`tempo-cycle`；操作：`seek`。

### 主动弃牌

主动弃牌，触发弃牌收益或整理手牌。

**基础关注点：** 主动弃牌

- 主动弃牌，触发弃牌收益或整理手牌。

维护 ID：`discard`；图谱：`discard-engine`；操作：`discard`。

### 本场消耗

消耗指定卡牌，换取一次性收益或压缩牌库。

**基础关注点：** 本场消耗

- 消耗指定卡牌，换取一次性收益或压缩牌库。

维护 ID：`exhaust`；图谱：`exhaust-engine`；操作：`exhaust`。

### 弃牌堆回收

从弃牌区回收卡牌，让关键组件再次使用。

**基础关注点：** 弃牌堆回收

- 从弃牌区回收卡牌，让关键组件再次使用。

维护 ID：`discard-recovery`；图谱：`graveyard-recovery`；操作：`recover`。

### 消耗区回收

从消耗区取回卡牌，把一次性资源变成有限循环。

**基础关注点：** 消耗区回收

- 从消耗区取回卡牌，把一次性资源变成有限循环。

维护 ID：`exhaust-recovery`；图谱：`exhaust-recovery`；操作：`recover`。

### 牌顶放置

把卡牌放到牌库顶部，安排下一次抽牌。

**基础关注点：** 牌顶放置

- 把卡牌放到牌库顶部，安排下一次抽牌。

维护 ID：`topdeck`；图谱：`topdeck-control`；操作：`move_card`。

### 牌底放置

把卡牌放到牌库底部，暂时避开不需要的牌。

**基础关注点：** 牌底放置

- 把卡牌放到牌库底部，暂时避开不需要的牌。

维护 ID：`bottomdeck`；图谱：`topdeck-control`；操作：`move_card`。

### 指定牌区移动

移动卡牌所在区域，配合后续区域触发。

**基础关注点：** 指定牌区移动

- 移动卡牌所在区域，配合后续区域触发。

维护 ID：`zone-move`；图谱：`tempo-cycle`；操作：`move_card`。

### 手牌保留

保留关键牌到下回合，等待费用或条件成熟。

**基础关注点：** 手牌保留

- 保留关键牌到下回合，等待费用或条件成熟。

维护 ID：`retain`；图谱：`retain-engine`；操作：`card_rule`。

### 牌组精简

移除低效卡牌，提高核心卡出现的比例。

**基础关注点：** 牌组精简

- 移除低效卡牌，提高核心卡出现的比例。

维护 ID：`card-removal`；图谱：`thin-deck`；操作：`remove_card`。

### 出牌后去向

指定卡牌去向，控制它进入哪个牌区。

**基础关注点：** 出牌后去向

- 指定卡牌去向，控制它进入哪个牌区。

维护 ID：`card-destination`；图谱：`rule-control`；操作：`card_destination`。

## 费用与出牌

选择费用、重放与自动结算机制。

### 临时减费

降低指定卡牌费用，让关键牌更早或更多次打出。

**基础关注点：** 临时减费

- 降低指定卡牌费用，让关键牌更早或更多次打出。

维护 ID：`cost-reduction`；图谱：`cost-shift`；操作：`reduce_cost`。

### 额外支付

为卡牌声明额外费用，出牌前完成原费用及额外费用的校验与支付。

**基础关注点：** 额外支付

- 为卡牌声明额外费用，出牌前完成原费用及额外费用的校验与支付。

维护 ID：`additional-payment`；图谱：`cost-shift`；操作：。

### 替代支付

为卡牌声明替代支付路径，出牌前选择一条可支付路径完成校验与支付。

**基础关注点：** 替代支付

- 为卡牌声明替代支付路径，出牌前选择一条可支付路径完成校验与支付。

维护 ID：`alternative-payment`；图谱：`cost-shift`；操作：。

### 免费出牌

让指定卡牌免费结算，把费用留给其他行动。

**基础关注点：** 免费出牌

- 让指定卡牌免费结算，把费用留给其他行动。

维护 ID：`free-play`；图谱：`zero-cost-engine`；操作：`card_rule`。

### 可变费用

按投入资源决定效果规模，用储备换取弹性。

**基础关注点：** 可变费用

- 按投入资源决定效果规模，用储备换取弹性。

维护 ID：`x-cost`；图谱：`x-cost-engine`；操作：。

### 卡牌回响

重复结算已打出的卡牌，再次触发其效果。

**基础关注点：** 卡牌回响

- 重复结算已打出的卡牌，再次触发其效果。

维护 ID：`replay`；图谱：`replay-chain`；操作：`replay_current`。

### 下次双重结算

让下一张符合条件的牌结算两次，集中打出爆发。

**基础关注点：** 下次双重结算

- 让下一张符合条件的牌结算两次，集中打出爆发。

维护 ID：`double-effect`；图谱：`replay-chain`；操作：`double`。

### 自动出牌

自动打出符合条件的卡牌，减少操作并维持循环。

**基础关注点：** 自动出牌

- 自动打出符合条件的卡牌，减少操作并维持循环。

维护 ID：`auto-play`；图谱：`auto-play-engine`；操作：`auto_play`。

### 出牌限制

限制卡牌何时或如何打出，换取更明确的节奏。

**基础关注点：** 出牌限制

- 限制卡牌何时或如何打出，换取更明确的节奏。

维护 ID：`play-restriction`；图谱：`rule-control`；操作：`card_rule`。

### 能量获取

获得能量，增加本回合可执行的行动。

**基础关注点：** 能量获取

- 获得能量，增加本回合可执行的行动。

维护 ID：`energy-gain`；图谱：`resource-engine`；操作：`energy`。

### 能量重置

把能量设为指定值，重新安排本回合费用。

**基础关注点：** 能量重置

- 把能量设为指定值，重新安排本回合费用。

维护 ID：`energy-reset`；图谱：`resource-engine`；操作：`set_energy`。

### 费用改造

修改指定卡牌费用，长期调整牌组节奏。

**基础关注点：** 费用改造

- 修改指定卡牌费用，长期调整牌组节奏。

维护 ID：`cost-patch`；图谱：`cost-shift`；操作：`patch_card`。

## 自定义资源

选择资源生成、存储、支付与重置。

### 资源获取

生成并获得自定义资源，为后续效果准备储备。

**基础关注点：** 资源获取

- 生成并获得自定义资源，为后续效果准备储备。

维护 ID：`resource-gain`；图谱：`resource-engine`；操作：`resource`。

### 资源消耗

支付自定义资源，换取对应的强力效果。

**基础关注点：** 资源消耗

- 支付自定义资源，换取对应的强力效果。

维护 ID：`resource-spend`；图谱：`resource-cashout`；操作：`resource`。

### 资源蓄积

保留资源跨回合积累，把储备转成未来爆发。

**基础关注点：** 资源蓄积

- 保留资源跨回合积累，把储备转成未来爆发。

维护 ID：`resource-store`；图谱：`resource-hoard`；操作：`resource`。

### 资源重置

把资源设为指定值，直接调整当前储备。

**基础关注点：** 资源重置

- 把资源设为指定值，直接调整当前储备。

维护 ID：`resource-reset`；图谱：`resource-engine`；操作：`set_resource`。

### 资源容量

提高或利用资源上限，让储备可以容纳更多。

**基础关注点：** 资源容量

- 提高或利用资源上限，让储备可以容纳更多。

维护 ID：`resource-capacity`；图谱：`resource-hoard`；操作：。

### 多资源支付

同时支付多种资源，用组合储备换取更高收益。

**基础关注点：** 多资源支付

- 同时支付多种资源，用组合储备换取更高收益。

维护 ID：`multiple-resource-cost`；图谱：`multi-resource`；操作：。

## 卡牌生成与成长

选择子卡、实例改造、附着和永久成长。

### 生成子卡

生成完整可用的子卡，补充当前需要的行动。

**基础关注点：** 生成子卡

- 生成完整可用的子卡，补充当前需要的行动。

维护 ID：`generated-card`；图谱：`generated-card-engine`；操作：`add_card`。

### 确保持有

确保指定卡牌进入牌组或牌区，稳定获得组件。

**基础关注点：** 确保持有

- 确保指定卡牌进入牌组或牌区，稳定获得组件。

维护 ID：`ensure-card`；图谱：`generated-card-engine`；操作：`ensure_card`。

### 复制卡牌

复制已有卡牌，扩大关键效果的出现次数。

**基础关注点：** 复制卡牌

- 复制已有卡牌，扩大关键效果的出现次数。

维护 ID：`copy-card`；图谱：`generated-card-engine`；操作：`copy`。

### 卡牌升级

升级指定卡牌，让它在后续循环中更高效。

**基础关注点：** 卡牌升级

- 升级指定卡牌，让它在后续循环中更高效。

维护 ID：`upgrade-card`；图谱：`card-evolution`；操作：`upgrade_card`。

### 本场卡牌修改

修改卡牌属性，让它适配当前构筑目标。

**基础关注点：** 本场卡牌修改

- 修改卡牌属性，让它适配当前构筑目标。

维护 ID：`modify-card`；图谱：`card-evolution`；操作：`modify_card`。

### 持有实例改造

只改造选中的那张牌，不自动影响它的其他副本。

**基础关注点：** 持有实例改造

- 只改造选中的那张牌，不自动影响它的其他副本。

维护 ID：`patch-card`；图谱：`card-evolution`；操作：`patch_card`。

### 未来副本改造

让改造也作用于之后生成的同模板副本，避免新牌失去强化。

**基础关注点：** 未来副本改造

- 让改造也作用于之后生成的同模板副本，避免新牌失去强化。

维护 ID：`future-copy-patch`；图谱：`card-evolution`；操作：`patch_card`。

### 卡牌变形

把卡牌变成另一张完整卡，改变后续行动路线。

**基础关注点：** 卡牌变形

- 把卡牌变成另一张完整卡，改变后续行动路线。

维护 ID：`transform-card`；图谱：`card-evolution`；操作：`transform_card`。

### 正面附着

给卡牌附加持续规则，让它获得额外能力。

**基础关注点：** 正面附着

- 给卡牌附加持续规则，让它获得额外能力。

维护 ID：`enchantment`；图谱：`enchantment-engine`；操作：`attach_card`。

### 负面附着

给卡牌附加负面规则，形成风险或交换收益。

**基础关注点：** 负面附着

- 给卡牌附加负面规则，形成风险或交换收益。

维护 ID：`affliction`；图谱：`affliction-engine`；操作：`attach_card`。

### 诅咒牌

加入会妨碍循环的诅咒牌，再围绕它设计处理手段。

**基础关注点：** 诅咒牌

- 加入会妨碍循环的诅咒牌，再围绕它设计处理手段。

维护 ID：`curse-card`；图谱：`curse-utilization`；操作：`add_card`。

### 干扰牌

主动制造牌库杂质，用数量或后续清理换取收益。

**基础关注点：** 干扰牌

- 主动制造牌库杂质，用数量或后续清理换取收益。

维护 ID：`deck-pollution`；图谱：`deck-pollution`；操作：`add_card`。

### 临时牌期限

控制卡牌进入、离开和返回牌区的时机。

**基础关注点：** 临时牌期限

- 控制卡牌进入、离开和返回牌区的时机。

维护 ID：`card-lifecycle`；图谱：`generated-card-engine`；操作：`add_card`。

### 按模板选牌

让效果精确筛选同一模板的卡牌，而不是依赖相似名称。

**基础关注点：** 按模板选牌

- 让效果精确筛选同一模板的卡牌，而不是依赖相似名称。

维护 ID：`template-selection`；图谱：`card-evolution`；操作：。

### 按实例选牌

让效果精确选中一张持有或战斗实例，不波及其他副本。

**基础关注点：** 按实例选牌

- 让效果精确选中一张持有或战斗实例，不波及其他副本。

维护 ID：`instance-selection`；图谱：`card-evolution`；操作：。

## 召唤

选择单位形态、行动、死亡与维护机制。

### 单核心

召唤一个核心单位，围绕它的生命和行动构筑。

**基础关注点：** 单核心

- 召唤一个核心单位，围绕它的生命和行动构筑。

维护 ID：`summon-single-core`；图谱：`summon-engine`；操作：`spawn_summon`。

### 多单位铺场

一次召唤多个单位，用数量和分工扩大场面。

**基础关注点：** 多单位铺场

- 一次召唤多个单位，用数量和分工扩大场面。

维护 ID：`summon-swarm`；图谱：`summon-swarm`；操作：`spawn_summon`。

### 自爆杂鱼

让召唤物主动牺牲，在离场时换取爆发收益。

**基础关注点：** 自爆杂鱼

- 让召唤物主动牺牲，在离场时换取爆发收益。

维护 ID：`summon-self-destruct`；图谱：`summon-sacrifice`；操作：`spawn_summon`、`dismiss_summon`、`damage`。

### 指挥行动

用指令激活或操控召唤物，集中安排它的行动。

**基础关注点：** 指挥行动

- 用指令激活或操控召唤物，集中安排它的行动。

维护 ID：`summon-command`；图谱：`summon-engine`；操作：`activate_summon`。

### 召唤被动

给召唤物持续能力，让它在满足事件时自动工作。

**基础关注点：** 召唤被动

- 给召唤物持续能力，让它在满足事件时自动工作。

维护 ID：`summon-passive`；图谱：`summon-engine`、`summon-passive-growth`；操作：`spawn_summon`。

### 生命计伤

读取召唤物生命来提高效果，让养大单位带来更高收益。

**基础关注点：** 生命计伤

- 读取召唤物生命来提高效果，让养大单位带来更高收益。

维护 ID：`summon-hp-scaling`；图谱：`summon-engine`、`summon-life-conversion`；操作：`spawn_summon`。

### 临时行动命令

给选中的召唤物下达一次临时行动，不改写它平时的招式。

**基础关注点：** 临时行动命令

- 给选中的召唤物下达一次临时行动，不改写它平时的招式。

维护 ID：`summon-ordered-action`；图谱：`summon-engine`、`summon-command-chain`；操作：`activate_summon`。

### 指定召唤出牌门槛

要求场上有指定召唤物，再让关联卡牌生效。

**基础关注点：** 指定召唤出牌门槛

- 要求场上有指定召唤物，再让关联卡牌生效。

维护 ID：`summon-required-card`；图谱：`summon-engine`、`summon-single-core`；操作：。

### 主动遣散

主动移除召唤物，腾出位置或配合离场触发能力。

**基础关注点：** 主动遣散

- 主动移除召唤物，腾出位置或配合离场触发能力。

维护 ID：`summon-dismiss`；图谱：`summon-sacrifice`；操作：`dismiss_summon`。

### 提前触发亡语

提前执行召唤物的亡语效果，无须等它真正死亡。

**基础关注点：** 提前触发亡语

- 提前执行召唤物的亡语效果，无须等它真正死亡。

维护 ID：`summon-death-repeat`；图谱：`summon-sacrifice`；操作：`trigger_summon_death`。

### 复制召唤物

复制已有召唤物，扩大单位能力或行动次数。

**基础关注点：** 复制召唤物

- 复制已有召唤物，扩大单位能力或行动次数。

维护 ID：`summon-copy`；图谱：`summon-swarm`；操作：`copy_summon`。

### 召唤物受损

伤害选中的召唤物，可与受伤或死亡触发能力配合。

**基础关注点：** 召唤物受损

- 伤害选中的召唤物，可与受伤或死亡触发能力配合。

维护 ID：`summon-hurt`；图谱：`summon-sacrifice`；操作：`damage_summon`。

### 召唤物治疗

治疗召唤物，延长它的在场时间。

**基础关注点：** 召唤物治疗

- 治疗召唤物，延长它的在场时间。

维护 ID：`summon-heal`；图谱：`summon-engine`；操作：`heal_summon`。

### 召唤物属性强化

修改召唤物属性，让它承担更明确的战斗职责。

**基础关注点：** 召唤物属性强化

- 修改召唤物属性，让它承担更明确的战斗职责。

维护 ID：`summon-stat`；图谱：`summon-engine`；操作：`modify_summon`。

### 召唤物行动强化

强化召唤物行动，让它每次激活产生更高收益。

**基础关注点：** 召唤物行动强化

- 强化召唤物行动，让它每次激活产生更高收益。

维护 ID：`summon-action-upgrade`；图谱：`summon-engine`；操作：`modify_summon_effect`。

### 召唤物状态

给召唤物施加状态，用状态控制它的表现。

**基础关注点：** 召唤物状态

- 给召唤物施加状态，用状态控制它的表现。

维护 ID：`summon-status`；图谱：`summon-engine`；操作：`apply_summon_status`。

### 召唤物净化

移除召唤物状态，清除限制或重新准备它。

**基础关注点：** 召唤物净化

- 移除召唤物状态，清除限制或重新准备它。

维护 ID：`summon-cleanse`；图谱：`summon-engine`；操作：`remove_summon_status`。

### 召唤物局部资源

给召唤物分配资源，让它按自身储备行动。

**基础关注点：** 召唤物局部资源

- 给召唤物分配资源，让它按自身储备行动。

维护 ID：`summon-resource`；图谱：`summon-engine`；操作：`summon_resource`。

### 召唤物资源重置

重置召唤物资源，直接安排下一次行动规模。

**基础关注点：** 召唤物资源重置

- 重置召唤物资源，直接安排下一次行动规模。

维护 ID：`summon-resource-reset`；图谱：`summon-engine`；操作：`set_summon_resource`。

### 主人效果

让召唤物把收益传回主人，形成单位与玩家的循环。

**基础关注点：** 主人效果

- 让召唤物把收益传回主人，形成单位与玩家的循环。

维护 ID：`summon-owner-effect`；图谱：`summon-engine`；操作：`summoner_effects`。

### 召唤模板永久成长

永久强化召唤模板，让后续战斗召唤出的单位也受益。

**基础关注点：** 召唤模板永久成长

- 永久强化召唤模板，让后续战斗召唤出的单位也受益。

维护 ID：`summon-growth`；图谱：`summon-engine`；操作：`persistent_growth`。

### 核心复苏

让已倒下的召唤物复活，维持核心单位循环。

**基础关注点：** 核心复苏

- 让已倒下的召唤物复活，维持核心单位循环。

维护 ID：`summon-revive`；图谱：`summon-engine`；操作：`spawn_summon`。

### 容量替换

召唤时指定容量与满员处理方式，决定替换旧单位还是拒绝加入。

**基础关注点：** 容量替换

- 召唤时指定容量与满员处理方式，决定替换旧单位还是拒绝加入。

维护 ID：`summon-capacity`；图谱：`summon-swarm`；操作：`spawn_summon`。

## 姿态

区分当前互斥姿态和可容纳多个姿态的槽位。

### 当前姿态切换

切换互斥的当前姿态，按顺序处理旧姿态退出和新姿态进入。

**基础关注点：** 当前姿态切换

- 切换互斥的当前姿态，按顺序处理旧姿态退出和新姿态进入。

维护 ID：`stance-switch`；图谱：`stance-engine`；操作：`stance`。

### 当前姿态修饰

当前姿态通过自身的被动效果提供持续修饰，仅在该姿态存续时适用。

**基础关注点：** 当前姿态修饰

- 当前姿态通过自身的被动效果提供持续修饰，仅在该姿态存续时适用。

维护 ID：`stance-passive`；图谱：`stance-engine`；操作：`stance`。

### 姿态进入触发

进入指定当前姿态时触发其进入能力。

**基础关注点：** 姿态进入触发

- 进入指定当前姿态时触发其进入能力。

维护 ID：`stance-entry`；图谱：`stance-engine`；操作：`stance`。

### 姿态退出触发

退出指定当前姿态时触发其退出能力。

**基础关注点：** 姿态退出触发

- 退出指定当前姿态时触发其退出能力。

维护 ID：`stance-exit`；图谱：`stance-engine`；操作：`stance`。

### 填充姿态槽

把具有完整定义的姿态填入姿态槽，使其被动按规则生效。

**基础关注点：** 填充姿态槽

- 把具有完整定义的姿态填入姿态槽，使其被动按规则生效。

维护 ID：`orb-channel`；图谱：`orb-engine`；操作：`channel_orb`。

### 主动激发姿态

激发指定槽位中的姿态，执行其激发效果并按规则离场。

**基础关注点：** 主动激发姿态

- 激发指定槽位中的姿态，执行其激发效果并按规则离场。

维护 ID：`orb-evoke`；图谱：`orb-engine`；操作：`evoke_orb`。

### 姿态内部值

修改槽内姿态的内部数值，供其被动或激发效果读取。

**基础关注点：** 姿态内部值

- 修改槽内姿态的内部数值，供其被动或激发效果读取。

维护 ID：`orb-modify`；图谱：`orb-engine`；操作：`modify_orb`。

### 姿态槽容量

增减可用姿态槽位，按容量变化规则处理已有姿态。

**基础关注点：** 姿态槽容量

- 增减可用姿态槽位，按容量变化规则处理已有姿态。

维护 ID：`orb-slots`；图谱：`orb-engine`；操作：`orb_slots`。

### 满槽挤出

填充已满的姿态槽时挤出旧姿态并触发其激发效果。

**基础关注点：** 满槽挤出

- 填充已满的姿态槽时挤出旧姿态并触发其激发效果。

维护 ID：`orb-overflow`；图谱：`orb-engine`；操作：`channel_orb`。

## 目标与条件

选择作用范围和需要判断的战场信息。

### 全体敌人

效果对全部合法敌人分别结算。

**基础关注点：** 全体敌人

- 效果对全部合法敌人分别结算。

维护 ID：`all-enemies`；图谱：`multi-target`；操作：。

### 随机敌人

每次选择从当前合法敌人中随机确定目标。

**基础关注点：** 随机敌人

- 每次选择从当前合法敌人中随机确定目标。

维护 ID：`random-enemy`；图谱：`multi-target`、`random-gamble`；操作：。

### 随机我方

随机我方目标包含玩家自己与存活的己方召唤物。

**基础关注点：** 随机我方

- 随机我方目标包含玩家自己与存活的己方召唤物。

维护 ID：`random-ally`；图谱：`random-gamble`；操作：。

### 最低生命目标

按合法选择器选择生命最低或生命比例最低的目标。

**基础关注点：** 最低生命目标

- 按合法选择器选择生命最低或生命比例最低的目标。

维护 ID：`lowest-hp`；图谱：`multi-target`；操作：。

### 手选目标

由玩家从合法候选中指定效果目标。

**基础关注点：** 手选目标

- 由玩家从合法候选中指定效果目标。

维护 ID：`manual-target`；图谱：`reactive-control`；操作：。

### 场上数量条件

按己方或敌方当前存活单位数量判断效果条件。

**基础关注点：** 场上数量条件

- 按己方或敌方当前存活单位数量判断效果条件。

维护 ID：`unit-count`；图谱：`reactive-control`；操作：。

### 条件保护

只有条件满足时执行受保护效果，保留条件与效果的关联。

**基础关注点：** 条件保护

- 只有条件满足时执行受保护效果，保留条件与效果的关联。

维护 ID：`conditional-guard`；图谱：`rule-control`；操作：`guard`。

### 效果分支自选

让玩家在多个可执行效果分支中选择，保留取消交互语义。

**基础关注点：** 效果分支自选

- 让玩家在多个可执行效果分支中选择，保留取消交互语义。

维护 ID：`effect-choice`；图谱：`reactive-control`；操作：`choose`。

### 减益控制

为目标赋予具有实际弱化或限制效果的状态。

**基础关注点：** 减益控制

- 为目标赋予具有实际弱化或限制效果的状态。

维护 ID：`negative-control`；图谱：`stall-control`；操作：`apply_status`。

## 事件与时机

选择触发来源、计数窗口和未来结算。

### 出牌触发

按实际出牌事件触发能力，区别于回响或效果内部命中。

**基础关注点：** 出牌触发

- 按实际出牌事件触发能力，区别于回响或效果内部命中。

维护 ID：`on-play`；图谱：`on-play-engine`；操作：。

### 命中触发

每次造成实际伤害时触发能力，不把零伤害当作有效命中。

**基础关注点：** 命中触发

- 每次造成实际伤害时触发能力，不把零伤害当作有效命中。

维护 ID：`on-hit`；图谱：`on-hit-engine`；操作：。

### 弃牌触发

在实际弃置卡牌后触发能力，并使用该次弃牌的信息。

**基础关注点：** 弃牌触发

- 在实际弃置卡牌后触发能力，并使用该次弃牌的信息。

维护 ID：`on-discard`；图谱：`discard-payoff`；操作：。

### 消耗触发

在卡牌实际进入消耗区后触发能力。

**基础关注点：** 消耗触发

- 在卡牌实际进入消耗区后触发能力。

维护 ID：`on-exhaust`；图谱：`exhaust-engine`；操作：。

### 击杀触发

由实际击杀者触发能力，区分玩家与召唤物的击杀归属。

**基础关注点：** 击杀触发

- 由实际击杀者触发能力，区分玩家与召唤物的击杀归属。

维护 ID：`on-kill`；图谱：`execute-finish`；操作：。

### 抽牌触发

在实际抽到卡牌时触发能力，未抽到牌不虚增事件。

**基础关注点：** 抽牌触发

- 在实际抽到卡牌时触发能力，未抽到牌不虚增事件。

维护 ID：`on-draw`；图谱：`draw-engine`；操作：。

### 回合开始触发

在指定所属方回合开始阶段触发能力。

**基础关注点：** 回合开始触发

- 在指定所属方回合开始阶段触发能力。

维护 ID：`turn-start`；图谱：`power-engine`；操作：。

### 回合结束触发

在指定所属方回合结束阶段触发能力。

**基础关注点：** 回合结束触发

- 在指定所属方回合结束阶段触发能力。

维护 ID：`turn-end`；图谱：`power-engine`；操作：。

### 本回合前几次

把指定触发器限制为本回合前若干次，回合变化后重置计数。

**基础关注点：** 本回合前几次

- 把指定触发器限制为本回合前若干次，回合变化后重置计数。

维护 ID：`first-n`；图谱：`rule-control`；操作：。

### 本场次数限制

把指定触发器限制为本场最多若干次，后续回合不重置。

**基础关注点：** 本场次数限制

- 把指定触发器限制为本场最多若干次，后续回合不重置。

维护 ID：`battle-uses`；图谱：`rule-control`；操作：。

### 伤害前拦截

让状态在 before_damage 时拦截该次伤害，可取消、替换或改写结算。

**基础关注点：** 伤害前拦截

- 让状态在 before_damage 时拦截该次伤害，可取消、替换或改写结算。

维护 ID：`damage-interception`；图谱：`rule-control`；操作：。

### 出牌前拦截

让状态在 before_card_play 时拦截该次出牌，可取消卡牌效果或替换为有限效果，已支付费用不退。

**基础关注点：** 出牌前拦截

- 让状态在 before_card_play 时拦截该次出牌，可取消卡牌效果或替换为有限效果，已支付费用不退。

维护 ID：`card-play-interception`；图谱：`rule-control`；操作：。

### 每隔几次触发

按指定事件的累计次数周期触发，明确计数窗口。

**基础关注点：** 每隔几次触发

- 按指定事件的累计次数周期触发，明确计数窗口。

维护 ID：`periodic-trigger`；图谱：`on-play-engine`；操作：。

### 事件实际值计量

效果读取本次事件的实际数值，区别于当前总量或预估值。

**基础关注点：** 事件实际值计量

- 效果读取本次事件的实际数值，区别于当前总量或预估值。

维护 ID：`event-value`；图谱：`on-hit-engine`；操作：。

### 预约效果

将效果预约到未来指定回合与阶段再执行。

**基础关注点：** 预约效果

- 将效果预约到未来指定回合与阶段再执行。

维护 ID：`schedule`；图谱：`delayed-payoff`；操作：`schedule`。

### 周期预约

预约效果按声明间隔重复执行，具有明确次数或终止条件。

**基础关注点：** 周期预约

- 预约效果按声明间隔重复执行，具有明确次数或终止条件。

维护 ID：`repeating-schedule`；图谱：`delayed-payoff`；操作：`schedule`。

### 额外回合

获得额外回合，依照回合队列和终态规则结算。

**基础关注点：** 额外回合

- 获得额外回合，依照回合队列和终态规则结算。

维护 ID：`extra-turn`；图谱：`extra-turn-engine`；操作：`extra_turn`。

### 主动结束回合

主动结束当前回合并进入正常回合末结算。

**基础关注点：** 主动结束回合

- 主动结束当前回合并进入正常回合末结算。

维护 ID：`end-turn`；图谱：`extra-turn-engine`；操作：`end_turn`。

## 图谱覆盖索引

| 图谱机制 | 对应流派 |
| --- | --- |
| 直接压制 | 即时伤害、穿透格挡 |
| 多段连击 | 多段伤害 |
| 倍率爆发 | 伤害倍率 |
| 斩杀终结 | 阈值处决、直接终结、击杀触发 |
| 形态联动 | 形态联动 |
| 状态积累 | 状态叠层、限时状态、状态复制、状态转移 |
| 持续伤害 | 持续伤害 |
| 状态引爆 | 状态净化、状态消耗 |
| 状态转化 | 状态层数计值 |
| 状态成长 | 状态层数计值、状态条件、持续属性修饰 |
| 敌方状态反哺 | 敌方状态响应 |
| 欲望压制 | 欲望施压、欲望降低、欲望重置 |
| 欲望溢出 | 欲望满溢、欲望上限永久成长 |
| 欲望转化 | 欲望计值 |
| 双轴压制 | 玩家自组：即时伤害 + 欲望施压 |
| 格挡循环 | 获得格挡、格挡重置 |
| 格挡留存 | 格挡留存 |
| 格挡转化 | 格挡计值 |
| 反击 | 受击反击 |
| 恢复循环 | 生命恢复、生命设值、满生命条件、生命上限成长 |
| 恢复转化 | 吸血生存 |
| 自伤交换 | 主动自伤 |
| 残血爆发 | 已损生命计值、低生命条件 |
| 抽牌引擎 | 抽牌、抽牌触发 |
| 快速轮转 | 定向检索、指定牌区移动 |
| 弃牌引擎 | 主动弃牌 |
| 弃牌收益 | 弃牌触发 |
| 消耗引擎 | 本场消耗、消耗触发 |
| 消耗回收 | 消耗区回收 |
| 弃牌堆回收 | 弃牌堆回收 |
| 牌顶控制 | 占卜、牌顶放置、牌底放置 |
| 牌库精简 | 牌组精简 |
| 零费连锁 | 免费出牌 |
| 费用操纵 | 临时减费、额外支付、替代支付、费用改造 |
| X费爆发 | 可变费用 |
| 保留蓄势 | 手牌保留 |
| 回响连锁 | 卡牌回响、下次双重结算 |
| 自动出牌 | 自动出牌 |
| 出牌触发 | 出牌触发、每隔几次触发 |
| 命中触发 | 命中触发、事件实际值计量 |
| 持续能力 | 持续能力、回合开始触发、回合结束触发 |
| 延迟收益 | 预约效果、周期预约 |
| 资源引擎 | 能量获取、能量重置、资源获取、资源重置 |
| 资源蓄积 | 资源蓄积、资源容量 |
| 资源兑现 | 资源计值、资源消耗 |
| 多资源编织 | 多资源支付 |
| 衍生牌 | 生成子卡、确保持有、复制卡牌、临时牌期限 |
| 卡牌成长 | 卡牌升级、本场卡牌修改、持有实例改造、未来副本改造、卡牌变形、按模板选牌、按实例选牌 |
| 附魔构筑 | 正面附着 |
| 负面附着 | 负面附着 |
| 诅咒利用 | 诅咒牌 |
| 牌库污染 | 干扰牌 |
| 姿态切换 | 当前姿态切换、当前姿态修饰、姿态进入触发、姿态退出触发 |
| 姿态槽循环 | 填充姿态槽、主动激发姿态、姿态内部值、姿态槽容量、满槽挤出 |
| 召唤协同 | 单核心、指挥行动、召唤被动、生命计伤、临时行动命令、指定召唤出牌门槛、召唤物治疗、召唤物属性强化、召唤物行动强化、召唤物状态、召唤物净化、召唤物局部资源、召唤物资源重置、主人效果、召唤模板永久成长、核心复苏 |
| 召唤群攻 | 多单位铺场、复制召唤物、容量替换 |
| 召唤献祭 | 自爆杂鱼、主动遣散、提前触发亡语、召唤物受损 |
| 单核心养成 | 指定召唤出牌门槛 |
| 召唤被动成长 | 召唤被动 |
| 召唤生命转伤 | 生命计伤 |
| 召唤指令连动 | 临时行动命令 |
| 多目标压制 | 全体敌人、随机敌人、最低生命目标 |
| 意图反制 | 手选目标、场上数量条件、效果分支自选 |
| 规则控制 | 出牌后去向、出牌限制、条件保护、本回合前几次、本场次数限制、伤害前拦截、出牌前拦截 |
| 拖延控制 | 减益控制 |
| 额外回合 | 额外回合、主动结束回合 |
| 随机博弈 | 随机敌人、随机我方 |

## 操作覆盖与范围

预设覆盖 64 个玩家可用操作，其余公共操作的范围说明：

- `narrate`：叙事输出不改变战斗机制，不能作为构筑基础。
- `say`：对白提示不改变战斗机制，不能作为构筑基础。
- `wait`：等待属于流程控制，不单独形成构筑方向。
- `enemy_intent`：敌方意图是敌人内容，玩家通过目标、条件或防护机制应对。
- `spawn_enemy`：正式敌方增援属于敌人内容；玩家可选择多目标与场上数量条件来应对。
