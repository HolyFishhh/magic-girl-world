# 0.5.97 测试记录

## 首轮初始化触发修复

- `[开始游戏]` 直接激活首轮 MVU 初始化规范，不再只依赖剧情模型回显 `<CHARACTER_INIT_PENDING>`。
- 用户绕过开始页表单、直接自由输入时同样必须完成初始化；缺少预设字段由剧情事实补全。
- `battle.cards` 为空时，完整初始化优先于普通增量更新，禁止只更新时间、地点、职业或核心数值。
- 首轮直接进入战斗时，玩家初始化与完整 `battle.enemy` 必须在同一次 MVU 更新中完成。

本版本仅用于本地真实酒馆验收，验收完成前不推送远端仓库。
