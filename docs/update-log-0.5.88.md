# 0.5.88 更新记录

- 修复 MVU 额外模型把变量更新写成 `<Update>/<Set>/<Add>` 嵌套 XML，导致整块更新被拒绝的问题。
- 新增只在 `[mvu_update]` 变量轮次注入的短格式契约，复杂对象统一使用 `_.set/_.assign` 函数命令；剧情模型不会额外承担这段提示词。
- 变量更新规则改为常驻蓝灯条目，确保额外模型稳定收到当前协议。
- 角色显示名和内嵌世界书升级为 `魔法少女世界 0.5.88` / `魔法少女世界0.5.88`，用于绕开 SillyTavern 对旧同名卡的脚本与世界书缓存。
- 根目录仍只交付一个 `魔法少女世界.png`，导入后请确认内嵌世界书并启用当前角色脚本。
