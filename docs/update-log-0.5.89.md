# 0.5.89 更新记录

- 修复角色卡首条消息把 `[开始游戏]` 包在 Markdown 代码围栏内，导致开始模块替换后形成嵌套代码块、首页 iframe 报 `Unexpected token '<'` 的问题。
- 发布卡首条消息统一为裸 `[开始游戏]`，开始模块只生成一层可执行 HTML 代码围栏。
- 实机启动测试改为读取导入角色卡的真实 `first_mes`，避免测试消息与最终角色卡不一致。
- 角色显示名和内嵌世界书升级为 `魔法少女世界 0.5.89` / `魔法少女世界0.5.89`，避免酒馆复用已导入的 `0.5.88` 缓存。
