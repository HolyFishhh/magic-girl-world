/** Card play gate, distinct from a conditional effect after paying the cost. */
export declare function describeSummonPlayRequirement(id: unknown, names?: Readonly<Record<string, string>>): string | undefined;
