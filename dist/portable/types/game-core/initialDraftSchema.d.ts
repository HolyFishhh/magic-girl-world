import { type TowerJsonSchema } from './towerRequest';
/** Reuse the authoritative effects grammar; change definition placement only. */
export declare function createInitialDraftJsonSchema(options?: {
    includeNarrative?: boolean;
}): TowerJsonSchema;
