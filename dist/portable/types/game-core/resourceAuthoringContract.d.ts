/** Public compact examples. Tests execute the JSON actually rendered in the prompt. */
export declare function formatCombatResourceAuthoringContract(placement?: 'runtime' | 'initial-draft'): string;
/** Shared by the raw prompt and the checked worldbook resource section. */
export declare function formatCombatResourceExecutionContract(): string;
