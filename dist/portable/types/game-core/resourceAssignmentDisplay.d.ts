/** Display-only shorthand: collapse a half only when it reads the resource being assigned. */
export declare function describeResourceHalf(value: unknown, owner: string, id: string): string | undefined;
