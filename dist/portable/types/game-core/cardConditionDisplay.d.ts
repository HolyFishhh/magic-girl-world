/** Display only; event-result predicates are not current hand membership. */
export declare function describeCardTypeCondition(kind: 'last_card_type' | 'discarded_card_type', type: string, negated?: boolean): string;
